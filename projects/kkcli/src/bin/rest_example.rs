// This example previously used the external keepkey-rest crate, which is now internalized.
// The file is left as a placeholder for future REST API examples using the internal kkcli REST server.

// Example removed: keepkey-rest crate is no longer a dependency.
